1--var allow to declear in same skope but const and let not allow to dealare variable in same in same skope
2--spread operator expends array or object into a single element
3--map () transformed eatch element filter select element then meet the condition foreatch itarate the full elements
4---array function  is the shorter vertion of  traditional function  its also allow multyline using backtick '
5-- String literals allowing interpolation and multi-line strings using backticks
